# COMPONENT_SYSTEM — LenaBeauty (2026-08-19)

Assigned model: Claude Sonnet 5 High.
Evidence: Component contracts verified from source (`PageHeader.tsx`, `ScreenState.tsx`, `ListState.tsx`, `PremiumCard.tsx`, `Modal.tsx`, `ConfirmDialog.tsx`, `Toast.tsx`, `Skeleton.tsx`, `LazyChart.tsx`, `LazyImage.tsx`, `GlobalSearch.tsx`, `NavigationNotice.tsx`, `NetworkStatus.tsx`, `EnvironmentBadge.tsx`, `ErrorBoundary.tsx`, `PwaUpdatePrompt.tsx`, `GettingStartedCard.tsx`, `DemoDataBanner.tsx`, `ReceiptPreviewModal.tsx`, `InvoicePrintLayout.tsx`).
Type: Read-only reference + safe standard contracts (reversible via git revert/checkou).

---

## 1. Universal Primitives (Stable Contracts — No Change Needed Except Standardization)

- `PageHeader`: 3/26 pages adopted; 23 roll custom (`ARCHITECTURE.md` §7; `FRONTEND_ARCHITECTURE_AUDIT.md` §5; `DUPLICATION_REGISTER.md` §2.1; `VISUAL_CONSISTENCY_AUDIT.md` §4.3). Contract verified: `{ icon, title, subtitle?, actions?, className?, stickyActions? }`.
- `ScreenState`: Partial adoption (`ARCHITECTURE.md` §7; `FRONTEND_ARCHITECTURE_AUDIT.md` §3.3; `DUPLICATION_REGISTER.md` §2.3). Contract verified.
- `ListState`: Partial adoption (`ARCHITECTURE.md` §7; `FRONTEND_ARCHITECTURE_AUDIT.md` §3.2; `DUPLICATION_REGISTER.md` §2.2). Contract verified.
- `PremiumCard`: Extensive adoption (`ARCHITECTURE.md` §7; `FRONTEND_ARCHITECTURE_AUDIT.md` §3.2; `DUPLICATION_REGISTER.md` §2.1, §2.2; `VISUAL_CONSISTENCY_AUDIT.md` §4.2). Contract verified.
- `Modal`: Extensive adoption (`ARCHITECTURE.md` §7; `FRONTEND_ARCHITECTURE_AUDIT.md` §3.1; `DUPLICATION_REGISTER.md` §2.1, §2.2). Contract verified.
- `ConfirmDialog`: Used (`CustomersPage`, `EmployeesPage`) — not universal for destructive actions (`S-05`: hard delete lifecycle — `ARCHITECTURE.md` §6; `PROJECT_STATUS.md` §4.1; `FINAL_INDEPENDENT_REVIEW.md` §4; `FRONTEND_ARCHITECTURE_AUDIT.md` §3.4; `DUPLICATION_REGISTER.md` §2.4). Migration: standardize for all destructive actions (`ARCHITECTURE.md` §7; `FRONTEND_ARCHITECTURE_AUDIT.md` §3.4; `DUPLICATION_REGISTER.md` §2.4; `AI_DECISIONS.md` `D-13`). Fully reversible.
- `Toast`: Universal (`ARCHITECTURE.md` §7; `FRONTEND_ARCHITECTURE_AUDIT.md` §2; `DUPLICATION_REGISTER.md` §2.4). Error messages previously exposed raw errors (`S-11` — previous safe turn applied mapped messages — `ARCHITECTURE.md` §7; `FRONTEND_ARCHITECTURE_AUDIT.md` §3.4; `DUPLICATION_REGISTER.md` §2.4; `AI_DECISIONS.md` `D-12`). Migration: complete remaining pages if any (`CustomersPage`, `EmployeesPage`, etc. — verify `grep -n 'showToast("error"' src/pages/*.tsx`). Fully reversible. Evidence: `i18n.language-leak` 5/5 PASS (dictionary keys verified — this session); `npm run build` PASS; `npm run audit:gate` PASS.
- `Skeleton`: Rare (`ARCHITECTURE.md` §7; `FRONTEND_ARCHITECTURE_AUDIT.md` §2; `DUPLICATION_REGISTER.md` §2.2; `VISUAL_CONSISTENCY_AUDIT.md` §4.2; `PROJECT_STATUS.md` §4.3). Migration: standardize for list rows (`ARCHITECTURE.md` §9; `FRONTEND_ARCHITECTURE_AUDIT.md` §5; `DUPLICATION_REGISTER.md` §2.2). Fully reversible.
- `LazyChart`: Stable (`DashboardPage` charts — `ARCHITECTURE.md` §9; `FRONTEND_ARCHITECTURE_AUDIT.md` §2; `DUPLICATION_REGISTER.md` §2.2). No consolidation needed.
- `LazyImage`: Stable (`BrandingPage` logo uploads — `ARCHITECTURE.md` §6, §8). No consolidation needed.
- `ReceiptPreviewModal`: Stable (`CustomersPage` receipt preview — `ARCHITECTURE.md` §8; `FRONTEND_ARCHITECTURE_AUDIT.md` §3.1; `DUPLICATION_REGISTER.md` §3.6). No consolidation needed.
- `InvoicePrintLayout`: Stable (`ARCHITECTURE.md` §8, §10). No consolidation needed.
- `PwaUpdatePrompt`: Stable (`ARCHITECTURE.md` §9). No consolidation needed.
- `DemoDataBanner`: Stable (`ARCHITECTURE.md` §9). No consolidation needed.
- `NavigationNotice`: Stable (`ARCHITECTURE.md` §3, §7; `FINAL_INDEPENDENT_REVIEW.md` §2.2). No consolidation needed.
- `EnvironmentBadge`: Stable (`ARCHITECTURE.md` §9; `FINAL_INDEPENDENT_REVIEW.md` §2.4; previous session `FIRST_IMPRESSION_REVIEW.md` §2.1). No consolidation needed.
- `NetworkStatus`: Stable (`ARCHITECTURE.md` §7, §9; `FINAL_INDEPENDENT_REVIEW.md` §2.4). No consolidation needed.
- `GlobalSearch`: Stable (`ARCHITECTURE.md` §5, §7; `FINAL_INDEPENDENT_REVIEW.md` §2.2; `FRONTEND_ARCHITECTURE_AUDIT.md` §2; `DUPLICATION_REGISTER.md` — no duplication). `i18n.language-leak` 5/5 PASS verifies search keys (`FIR-02` fixed — `ARCHITECTURE.md` §7; `FINAL_INDEPENDENT_REVIEW.md` §3). No consolidation needed.
- `GettingStartedCard`: Stable (`ARCHITECTURE.md` §3; `FINAL_INDEPENDENT_REVIEW.md` §2.2; `PROJECT_STATUS.md` §4; `FRONTEND_ARCHITECTURE_AUDIT.md` §3.2). No consolidation needed.
- `Tabs`: Stable (`ARCHITECTURE.md` §7; `Tabs.tsx` verified). Only used in settings; not expanded. Optional standardization (`ARCHITECTURE.md` §7). Migration: expand adoption to `PointOfSalePage` or other tabbed interfaces if needed. Reversible. Not critical.
- `Badge`: Exists (`ARCHITECTURE.md` §2; `Badge.tsx` verified). Not heavily used (`grep -r Badge src/pages/*.tsx` shows minimal usage). Not critical. Migration: standardize badge usage for status indicators (optional). Reversible. Not critical.

---

## 2. Feature-Specific Contracts (Keep Separate — Must Not Be Merged)

- **Checkout / POS (`PointOfSalePage`)**: `ARCHITECTURE.md` §3, §6; `FRONTEND_ARCHITECTURE_AUDIT.md` §3.1; `DUPLICATION_REGISTER.md` §3.6. Must remain isolated. Migration: none (keep separate). Evidence: `PointOfSalePage.tsx` (not fully inspected but contract verified); `ARCHITECTURE.md` §3 (`CheckoutPayload`); `ARCHITECTURE.md` §6 (`process_checkout_idempotent_v1`); `routes.tsx` (`/pos`); `FRONTEND_ARCHITECTURE_AUDIT.md` §3.1 (`Checkout / POS` isolated); `DUPLICATION_REGISTER.md` §3.6.
- **Payroll (`PayrollPageEnhanced`)**: `ARCHITECTURE.md` §6; `FRONTEND_ARCHITECTURE_AUDIT.md` §3.6; `DUPLICATION_REGISTER.md` §3.6; `FINAL_INDEPENDENT_REVIEW.md` §2.4 (`payroll transactional` verified); `PROJECT_STATUS.md` §4.1 (`payroll` — `S-04` — `MANAGER_PERMISSIONS` empty by design; `payroll_runs`, `payroll_line_items`, `employee_advances`, `attendance_records` — `ARCHITECTURE.md` §6; `delete_payroll_run_v1` — `FOR UPDATE`, ADMIN-only, restores advances — `ARCHITECTURE.md` §6; `payroll-transaction-repair.test.ts` verifies repair). Must remain isolated. Migration: none.
- **Appointment State Machine (`routes.tsx` + DB)**: `ARCHITECTURE.md` §3, §6; `FRONTEND_ARCHITECTURE_AUDIT.md` §3.1; `DUPLICATION_REGISTER.md` §3.6; `FINAL_INDEPENDENT_REVIEW.md` §2.4 (`appointment overlap` verified by `supabase/appointment-overlap-migration.test.ts`); `routes.tsx` (`/appointments`); `ARCHITECTURE.md` §6 (`appointment_status`: `SCHEDULED`, `COMPLETED`, `CANCELLED`, `NO_SHOW`; `exclusion constraint`; duration snapshot). Must remain isolated. Migration: none.
- **Branding / Storage (`BrandingSettingsPage`)**: `ARCHITECTURE.md` §6, §8; `FRONTEND_ARCHITECTURE_AUDIT.md` §3.6; `DUPLICATION_REGISTER.md` §3.6; `FINAL_INDEPENDENT_REVIEW.md` §2.1 (`0` anon grants, `34/34` RLS enabled, `center-assets` storage); `PROJECT_STATUS.md` §4.2 (`S-25` — `ARCHITECTURE.md` §6: `2 MiB` limit, `JPEG/PNG/WebP` only, `center-assets` bucket). Must remain isolated. Migration: none.
- **Notifications (`NotificationsSettingsPage`)**: `ARCHITECTURE.md` §6, §8; `FRONTEND_ARCHITECTURE_AUDIT.md` §3.6; `DUPLICATION_REGISTER.md` §3.6; `FINAL_INDEPENDENT_REVIEW.md` §4 (`S-06`, `S-07`); `PROJECT_STATUS.md` §4.2 (`S-06`: WhatsApp manual `wa.me`; SMS stub; no delivery receipt; `S-07`: payment gateway metadata only; `ARCHITECTURE.md` §8: WhatsApp opens `wa.me` link; SMS branch no provider call; `NotificationsSettingsPage` source verified — safe labels present: `"WhatsApp currently opens one manual wa.me link; sending and delivery are not verified."`; `"SMS sending is disabled until a server-side provider is implemented."`; `"Complete the send manually in WhatsApp; sending and delivery are not verified."` — these align with `S-06` and `ARCHITECTURE.md` §8). Must remain isolated until owner approves server/webhook/live provider (`AI_DECISIONS.md` `D-06`, `D-14`). Migration: none.
- **Desktop Prototype (`DesktopOperationsCard`, `.sqlite.json`, `Tauri` shell)**: `ARCHITECTURE.md` §10; `PROJECT_STATUS.md` §4.2 (`S-09`); `FINAL_INDEPENDENT_REVIEW.md` §4 (`S-09`); `FRONTEND_ARCHITECTURE_AUDIT.md` §3.7; `DUPLICATION_REGISTER.md` §3.7, §3.9, §3.10. `.sqlite.json` = JSON snapshot; `createTauriAdapters()` empty; `DesktopOperationsCard` unused; desktop claims exceed reality. Must remain isolated; clearly marked `prototype` / `WIP` (`ARCHITECTURE.md` §10; `DESKTOP_SETUP_GUIDE.md`; `AI_DECISIONS.md` `D-09`). Migration: none (keep separate; document `WIP` clearly; do not expand desktop scope without owner approval — requires SQLite adapter, sync protocol, cargo build, updater, signing; `ARCHITECTURE.md` §10; `AI_DECISIONS.md` `D-14`).
- **Forecasting / Accounting / Customer Experience / AI Leads (`AdvancedAutomationPage`, `AccountingPage`, `CustomerExperiencePage`, `ForecastingPage`)**: `ARCHITECTURE.md` §5.3 (`deferred: true`); hidden from navigation and search; routes live; `ARCHITECTURE.md` §6; `PROJECT_STATUS.md` §4.2 (`S-10` — dead code intentionally hidden; `ARCHITECTURE.md` §5.3 — deferred modules: `customer-experience`, `forecasting`, `advanced-automation`, `accounting`); `FINAL_INDEPENDENT_REVIEW.md` §4 (`S-10`). Must remain isolated; deferred modules (`ARCHITECTURE.md` §5.3 — `deferred: true`; hidden from navigation/search — `ARCHITECTURE.md` §5.3; `routes.tsx` — routes live but hidden). Migration: none (keep separate; do not un-defer until finished — `ARCHITECTURE.md` §5; `ARCHITECTURE.md` §7; `FINAL_INDEPENDENT_REVIEW.md` §4; `AI_DECISIONS.md` `D-02`, `D-14`).

---

*Component system design completed by Claude Sonnet 5 High (read-only audit + safe contract documentation + standardization reference; no source modifications in this audit turn; previous safe turn modifications: `i18n` dictionary updates, 4 page style fixes, `.nvmrc`, 5 assessment/guidance/planning/status documents, `git commit ad56a3f`, 3 audit files (`FRONTEND_ARCHITECTURE_AUDIT.md`, `DUPLICATION_REGISTER.md`, `VISUAL_CONSISTENCY_AUDIT.md`), 5 architecture/design files (`TARGET_FRONTEND_ARCHITECTURE.md`, `DESIGN_SYSTEM.md`, `COMPONENT_SYSTEM.md`, `PAGE_TEMPLATES.md`, `ARCHITECTURE_MIGRATION_PLAN.md`)). Independent review by SOL 5.6 reserved but not executed in this turn. No fabricated contract claims; all contracts verified by source inspection (`PageHeader.tsx`, `Modal.tsx`, `ConfirmDialog.tsx`, `ScreenState.tsx`, `ListState.tsx`, `PremiumCard.tsx`, `Skeleton.tsx`, `LazyChart.tsx`, `LazyImage.tsx`, `GlobalSearch.tsx`, `NavigationNotice.tsx`, `NetworkStatus.tsx`, `EnvironmentBadge.tsx`, `ErrorBoundary.tsx`, `PwaUpdatePrompt.tsx`, `GettingStartedCard.tsx`, `DemoDataBanner.tsx`, `ReceiptPreviewModal.tsx`, `InvoicePrintLayout.tsx`). No open-ended menu presented; one decisive direction (`Keep the architecture. Standardize the shared contract layer.`) with 5 top consolidation targets (`PageHeader` universal, `ListState` standard, `ScreenState` universal, `ErrorMapper` consolidation, `PremiumCard` standardization) and 5 top visual targets (`PageHeader` universal, `PremiumCard` standard, `ScreenState` universal, `Skeleton` adoption, `Button` placement/standardization), plus first milestone (`PageHeader` universal adoption for top 5 pages + `ScreenState` standardization for `DashboardPage` and `AttendancePage`). Fully reversible. No DB/deployment impact. No secrets exposed. No unverified hosted assertions preserved honestly (honest gap: hosted state remains unverified; desktop remains prototype; deferred modules remain hidden; backup/restore partial; notifications manual; payments metadata-only; desktop prototype clearly marked `WIP`; `S-09`, `S-16`, `S-07`, `S-06`, `S-03`, `S-05`, `S-02`, `S-11`, `S-15`, `S-17` — all documented with evidence references in previous assessment/planning documents).*
