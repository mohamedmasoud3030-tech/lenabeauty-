# 03 — Verified Drift Register

Machine-readable source: [`artifacts/audit-findings.json`](./artifacts/audit-findings.json), regenerated by `npm run audit:all`.

## Current result (2026-08-16)

- Migration replay failures: **0**
- Migration idempotency failures: **0**
- Repeat-replay fingerprint drift: **none**
- Blocking contract findings: **0**
- High / medium / low findings: **0 / 0 / 0**
- Informational findings: **4**

## Closed findings

- Payroll/attendance/advance tables are now center-role governed and ADMIN-only, matching the shipped route guard.
- Public booking and portal RPCs remain deliberately deny-by-default. Their adapters are retained for a future release, but no route is registered and no client role has EXECUTE.
- Previously non-idempotent policy migrations now replay cleanly; SECURITY DEFINER hardening remains stable on repeat replay.
- Tenant-scoped foreign keys for payroll, service categories, and payments are validated. Equivalent simple foreign keys were removed to avoid ambiguous PostgREST relationships.
- Internal entitlement/validation trigger routines have no client EXECUTE grant.
- A canonical generated `Database` type is committed and threaded through `SupabaseClient<Database>`; `npm run db:types:check` detects drift.

## Informational findings

1. Dormant public booking/portal RPCs are correctly disabled.
2. The scanner reports `printService.ts`'s non-Supabase `.from(element)` call for manual review; it is an html2pdf API call, not dynamic database access.
3. PGlite cannot execute the canonical `btree_gist` exclusion constraint; the replay uses a documented surrogate while PostgreSQL/Supabase installs the real constraint.
4. JSON-returning RPCs require runtime shape validation in addition to generated SQL signatures. Existing repository mappers/checks remain the runtime boundary.

The executable release gate is `npm run audit:gate`.
